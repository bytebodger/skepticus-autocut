{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "term_card props",
  "type": "object",
  "properties": {
    "term": { "type": "string", "description": "The word or phrase being defined." },
    "definition": { "type": "string", "description": "Its definition; reveals by line." }
  },
  "required": ["term", "definition"],
  "additionalProperties": false
}
