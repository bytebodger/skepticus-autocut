{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "title_card props",
  "type": "object",
  "properties": {
    "title": { "type": "string", "description": "Section title, set large and centered." },
    "subtitle": { "type": "string", "description": "Optional kicker above the title." }
  },
  "required": ["title"],
  "additionalProperties": false
}
