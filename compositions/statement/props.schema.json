{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "statement props",
  "type": "object",
  "properties": {
    "text": { "type": "string", "description": "The host's own words, set large. No quote marks, no attribution." }
  },
  "required": ["text"],
  "additionalProperties": false
}
